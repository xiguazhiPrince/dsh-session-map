//#region src/index.ts
/**
* Host half. This bundle contributes no host-side behaviour — the whole feature
* is a browser plugin — so this module exists only because the patch row names
* the package and the Loader imports its node half.
*/
/** Host plugin body: nothing to contribute host-side. */
function apply() {}
//#endregion
export { apply };

//# sourceMappingURL=index.js.map